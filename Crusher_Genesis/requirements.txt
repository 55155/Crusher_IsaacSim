# =============================================================================
# Crusher_Genesis — Python 의존성 (Genesis PBD 약봉투 파지/이송 시뮬)
# =============================================================================
#
# 현재 환경 (Windows + CUDA, 실제 개발/실행 환경 — 설치 절차는 PROJECT_NOTES.md):
#   miniconda3 env `crusher_genesis`, RTX 5080 (sm_120, 드라이버 595.79+ 필수)
#
# 검증 버전 (2026-08-19, 설치본에서 직접 확인):
#   Python 3.13.14 · genesis-world 1.3.3 · quadrants 1.3.0 · torch 2.11.0+cu128
#
# 구 환경 (2026-06, macOS/Metal — 이력 보존용, 지금은 쓰지 않음):
#   Framework Python 3.13 + Genesis 1.1.0 + Metal 백엔드
#     인터프리터: /Library/Frameworks/Python.framework/Versions/3.13/bin/python3
#     검증 버전: Python 3.13.5 · genesis-world 1.1.0 · torch 2.12.1 · numpy 2.2.6
#
# 비고:
#   - genesis-world 가 torch / gstaichi / coacd / opencv / imageio-ffmpeg /
#     moviepy / matplotlib / scipy / trimesh 등 대부분을 전이 의존성으로 끌어옴.
#     아래는 스크립트가 "직접" import 하는 것 + 버전 하한을 명시한 목록.
#   - mp4 카메라 렌더(Genesis rasterizer)는 GL 컨텍스트가 필요 → 디스플레이가
#     깨어 있어야 함(잠금/슬립 시 실패). 헤드리스 PBD 분포 출력은 GL 불필요.
# =============================================================================


# ── Genesis 물리 시뮬레이터 (PBD / Rigid / LegacyCoupler) ────────────────────
# torch, gstaichi, coacd, opencv-python, imageio-ffmpeg, moviepy 등을 함께 설치
genesis-world==1.3.3

# torch: Genesis 1.3.3 은 torch>=2.8 요구 (이하 버전은 unsupported 경고)
torch>=2.8


# ── 수치 연산 ────────────────────────────────────────────────────────────────
numpy>=1.26.4


# ── 메시 생성/처리 (봉투 STL 생성, 플레이트 로드) ─────────────────────────────
trimesh>=4.0


# ── 이미지 저장 (키프레임 PNG, 파티클 분포 figure) ───────────────────────────
Pillow>=11.0


# ── 시각화 (matplotlib 기반 grasp 뷰, box containment 애니메이션 GIF) ────────
matplotlib>=3.7

# ── 공간 질의 (ConvexHull / Delaunay 박스 포함 판정, cKDTree 응집력 분석) ─────
scipy>=1.10
