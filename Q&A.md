# Q&A — 2026-08-25 ~ 08-26 세션

이 세션에서 오간 질문과 답을 시간순으로 정리한다. 근거 수치를 같이 남긴다 —
결론만 남기면 나중에 다시 재봐야 하기 때문이다.

작업 브랜치: `cost-remeasure-genesis133` (HEAD `e192402`)

---

## 1. GitHub에서 변경사항 가져오기

> **Q.** github 에서 변경사항 가져와줘

`fetch` 결과 두 가지가 나왔다.

- `origin/cost-remeasure-genesis133` 이 **GitHub에서 삭제**돼 있었다. 그 커밋들은
  전부 `origin/rigid-process-validation` 에 흡수돼 있어서 거기서 fast-forward 했다.
- 새 커밋 1건: `e192402` "압착 공정 3건 규명 — 힘 상한 부재, 벽 형상 오독, 실링 두께 제약"

merge 를 막은 건 `_bag_seal_uv.obj` 였는데, 워크트리 해시가 HEAD와 동일해
(`f19b39f8`) 실제 수정이 아니라 LF/CRLF 개행 설정 때문이었다. 인덱스만 정리하고 진행.

---

## 2. "로컬 main이 7커밋 앞서 있다"는 게 무슨 뜻인가

> **Q.** 이게 무슨말이야? 로컬 변경사항이 더 앞서있다고? 지금 깃허브 상태랑 뭐가 다른지 알려줘

**로컬 전용 커밋은 하나도 없었다.** 앞선 답변에서 "푸시 안 됐다"고 쓴 건 부정확했다.
커밋은 전부 GitHub에 있었고, 뒤처진 건 GitHub의 `main` **브랜치 포인터**뿐이었다.

| | 커밋 | 날짜 | 최신 대비 |
|---|---|---|---|
| GitHub `main` | `3d96b23` | 8/11 | 19 커밋 뒤 |
| 로컬 `main` | `4b003ea` | 8/14 | 12 커밋 뒤 |
| 현재 브랜치 = `origin/rigid-process-validation` | `e192402` | 8/25 | 최신 |

세 포인터가 같은 일직선 위에 있었다. `git log --all --not --remotes` 가 빈 출력이라
로컬에만 있는 커밋이 0건임을 확인했다.

---

## 3. main 포인터 전진 + fusion2xml 위치

> **Q.** 메인브런치의 포인터도 앞당겨줘. 그리고 utils 에 내가 만든 fusion2xml이
> 없더라고 이거 주석 달아서 내 레포에 추가해줘

**main 전진**: `3d96b23` → `e192402`, 19커밋. 순수 fast-forward라 force 없이 push.

**fusion2xml**: 착오였다. `utils/fusion2xml/` 에 8개 파일 전부 있었고, push 하기
*전*의 GitHub `main`(`3d96b23`)에도 이미 있었다.

- 리포 루트에 **`utils/` 와 `utills/` 가 둘 다 존재**한다. 나머지 스크립트는 전부
  `utills/` 에 있고 `utils/` 안에는 fusion2xml 하나뿐이라 헷갈리기 쉽다.
- Fusion 360 애드인 폴더는 **junction** 이라 별도 사본이 아니다:
  `%APPDATA%\Autodesk\...\AddIns\fusion2xml` → `<repo>\utils\fusion2xml`
- 주석도 이미 충실했다(모듈·함수 docstring + 171줄 README).

---

## 4. Left_Wall 을 두 body 로 분리해 볼록분해를 피할 수 있는가

> **Q.** 2_Left_Wall의 플런지 형태를 `2_Left_wall_1` + `3_Left_wall_1` 두 body 로
> 분리해서, 하나로 묘사했을 때 collision mesh가 이상하게 볼록분해 되는 걸 방지해봤다.
> 분리하여 돌출된 플랜지 형태를 묘사하고자 하는데 가능한가?

**가능하다.** 신규 export 는 구조적으로 맞게 나왔다 — 조인트 없는 자식 body 는
MuJoCo에서 부모에 용접되므로 동역학은 한 덩어리, 충돌은 두 개의 독립 shape 다.

| | 실부피 | convex hull | hull 대비 |
|---|---|---|---|
| `2_Left_Wall1_1` (플랜지) | 5,850 mm³ | 5,850 mm³ | **100.0%** |
| `3_Left_Wall_1` (본체) | 32,273 | 116,090 | 27.8% |
| 합계 | **38,123** | — | — |
| 예전 통짜 `L2_Left_Wall1_1` | **38,123** | 136,890 | 27.8% |

부피가 소수점까지 같다. 플랜지는 verts 8개 = 정확한 18×5×65mm 직육면체.

**다만 분리만으로는 볼록분해 문제가 안 풀린다.** 본체는 여전히 ㅁ 프레임(27.8%)이라
단일 geom 으로 두면 hull 이 실부피의 3.6배가 되어 세로부재 사이 55mm 개구부
(impact plate 통로)를 메운다. 본체는 CoACD 를 계속 돌려야 한다.

**신규 export 를 그대로 쓰면 막히는 것 3건**

1. **벽 슬라이드 축이 틀렸다** — `48_0276 axis="0 0 1"` = 65mm 폭 방향.
   실제 구동축은 root x (가이드레일 60mm, 랙기어 120.18mm 가 x 방향).
2. **간격이 0.5mm 늘었다** — 새 CAD 의 Left_Wall 이 그만큼 물러나 있다.
   플랜지 12.00 → **12.50mm**, 본체 17.00 → **17.50mm**.
3. **§18-5 는 미구현** — 플랜지가 여전히 움직이는 벽의 자식이고, Wall3 상단보다
   1.00mm 위라 두 벽이 높이에서 안 겹치는 문제도 그대로.

---

## 5. Left_Wall 만 가져와서 적용

> **Q.** 그냥 다른건 참고하지 말고, Left_wall 쪽만 참고해서 사용해줘

조인트는 현재 모델 것을 유지하고 형상만 이식했다.

- 새 파일: `assets/MJCF/build_leftwall_split.py` + STL 7개
- 좌표 오프셋은 하드코딩하지 않고 공통 부품 4개(Wall1/Wall2/Wall3/PLATE)로
  유도 후 교차검증 — 네 부품이 `+27.124 / −159.074 / −1.423 mm` 로 완전 일치.
- `full_workflow.py` 에 `LEFTWALL_SPLIT`(기본 켜짐) + `_split_leftwall_collision()`

**중간에 낸 오답 정정**: "hull 11개가 XML에 연결돼 있지 않다"고 했는데 틀렸다.
실제 소스는 `paths.MJCF_MAIN` = **`Crusher_IsaacSim_colored.xml`** 인데
`Crusher_IsaacSim.xml` 을 읽고 있었다. colored 쪽에는 CoACD hull 11개가 이미
`group="3"` 충돌 geom 으로 배선돼 있었다. **즉 이 작업은 버그 수정이 아니라 개선이다.**

| | 기존 (hull 11) | 신규 (hull 5 + box 1) |
|---|---|---|
| 충돌 geom | 11 | **6** |
| 부피 충실도 | 38,847 mm³ (101.9%) | **38,694 mm³ (101.5%)** |
| 플랜지 | `hull_000` 5,895 (100.8%) | **정확한 box 5,850 (100.0%)** |
| AABB 겹치는 쌍 | 11 | **0** |

---

## 6. 두 body 로 나눈 걸 쓰고 있나? 강체 접합인가?

> **Q.** 현재 left_wall 이부분을 두개의 바디로 분할한거를 사용하고 있는거지?
> 연결은 강체 접합 시키고?

**아니다 — 바디가 아니라 geom 을 나눴다.** 기존 `L2_Left_Wall1_1` 한 바디 안의
geom 두 종류이고, 자식 바디도 조인트도 weld 제약도 없다. 같은 링크에 들어 있으니
강체 접합은 구조적으로 자동이다.

이렇게 한 이유: 볼록화는 **geom 단위**라 충돌 결과가 동일하고, 링크가 늘지 않아
구조 위험이 없다. §18-5 처럼 플랜지를 고정 프레임에 매달 때 진짜 body 로 올리면 된다.

---

## 7. 그린 게 STL 아니냐, Genesis 충돌메시는 다를 텐데

> **Q.** 너가 그린건 그냥 메쉬 파일이고 Genesis 상에서 비춰지는 충돌메쉬는
> 다를거같은데

**타당한 지적이었다.** 확인해보니 이번엔 같았지만, 확인 전엔 알 수 없는 게 맞다.
`scene.build()` 후 `geom.get_verts()` 로 솔버가 실제로 들고 있는 world 좌표를 뽑아
다시 그렸다.

| | 조각 수 | STL 합계 | Genesis 합계 | 차이 |
|---|---|---|---|---|
| 기존 | 11 | 38,847 mm³ | 38,847 mm³ | **0.0** |
| 신규 | 6 | 38,694 mm³ | 38,694 mm³ | **0.0** |

정점 수까지 1:1로 같았다. 이유는 둘 — **이미 볼록한 메시의 convex hull 은 자기
자신**이고, `decimate_face_num=500` 인데 조각들은 face 가 12~26개뿐이라 감축 대상이
아니다. 통짜 concave 메시였다면 여기서 hull 하나로 뭉개졌을 것이다.

---

## 8. Wall_1 과 Left_wall_1 이 맞물리는지 테스트

> **Q.** Wall_1 이랑 Left_wall_1 이 서로 맞물리는지 테스트를 해보자.
> Left_wall_1이 정확하게 Wall_1 맞물려야한다

정적 진단만으로 결론이 났다(폭·높이가 안 겹치면 q 를 아무리 줘도 못 만난다).

- **플랜지는 세 고정벽 전부와 높이가 안 겹친다.** `L2_Wall3_1` 과 −1.00mm,
  `L1_Wall1_1`/`L1_Wall2_1` 과 −67.00mm → **영영 못 만난다.**
- **본체 조각들은 `L2_Wall3_1` 과 겹친다** — x 간격 17.50mm 라 **q ≤ −17.50mm**
  에서 접촉.
- `L1_Wall1_1` 과 겹치는 건 본체 조각 하나뿐(폭 28.99, 높이 5.00mm).

즉 `CLAMP_TARGET=−12mm` 로는 본체가 Wall3 에 닿지 않고, 플랜지는 애초에 맞물릴 수
없다. §18-5 가 지적한 상태 그대로다.

---

## 9. Wall_1 의 충돌메시는 어떻게 되어 있나

> **Q.** 일단 Wall_1 쪽은 충돌메쉬가 어떻게 되어있는지 알려줘

| | 원본 STL | Genesis 충돌 |
|---|---|---|
| verts / faces | 480 / — | **27 / 50** |
| 부피 | 20,425 mm³ | **22,080 mm³** (1.08배) |
| 형상 | 볼트 구멍 6개 뚫린 평판 | **구멍이 메워진 통짜 슬래브** |

22,080 = 64 × 5 × 69 이므로 AABB 를 꽉 채운 직육면체와 같다. 다만 구멍은 내부
특징이고 Left_Wall 을 마주보는 면(world x = −336.28)은 원본과 정확히 같아
**맞물림 판정에는 영향이 없다.**

고정 구조물은 전부 볼록 hull 1개씩이고 손실이 1~12%뿐이다 — 원래 볼록에 가까운
판재라 Left_Wall(ㅁ 프레임, 3.6배)과 상황이 다르다.

| # | 부품 | verts | GS 부피 | 원본 | 부풀림 |
|---|---|---|---|---|---|
| 0 | `base_link` | 29 | 480,000 | 476,732 | 1.01배 |
| 2 | `L1_Wall1_1` | 27 | 22,080 | 20,425 | 1.08배 |
| 3 | `L1_Wall2_1` | 19 | 13,440 | 11,957 | 1.12배 |
| 4 | `L2_Wall3_1` | 25 | 79,200 | 77,599 | 1.02배 |

---

## 10. full_workflow 테스트

> **Q.** 일단 Full_sequence.py 한번 테스트 가자

```
[mjcf] 옛 geom 12개 -> 본체 hull 5 + 플랜지 1 + 시각 1   간격 12.50 / 17.50mm
[slot] gap_width=12.5mm            <- 슬롯 계산이 새 형상을 따라감 (12.0 -> 12.5)
[phase] clamp @done  wall=-8.25mm  (잔여: 플랜지면 4.25mm / 본체면 9.25mm)
[RESULT] verdict=PASS  오차=-1.0mm
[timing] build=16.4s  steps=250.4s (2430 steps, 103.1ms/step)
```

**IPC build 통과** — 유일하게 걸었던 리스크(`Intersection detected`)가 해소됐다.
스텝 시간 103.1ms 로 정상(발산 런은 4.7s/step).

단, `GRIP_OFFSET_MM` 기본값이 −28 이라 참조 런(+0mm)과 변인이 둘이고, 이 판정은
비결정적이라 한 번 PASS 로 개선을 주장할 수 없다.

---

## 11. Crusher 1분 운전

> **Q.** 이 상태에서 Crusher를 1분간 작동시키는 시퀀스를 수행시켜보자

`full_workflow.py` 에 **Phase 11 `crush`** 를 추가했다. 사양·패턴은 새로 정하지 않고
`Crusher_only.py` 에 확립된 것을 따랐다.

| 항목 | 값 | 근거 |
|---|---|---|
| 회전수 | 8 RPM (0.8378 rad/s) | `docs/Crusher.md` §1 |
| 제어 | velocity + 토크 클램프 | position 램프는 접촉 시 PD 가 무한 토크 |
| 토크 상한 | ±12.5 N·m | `Crusher.md` §2-2 |

기본 꺼짐(`CRUSH_SECONDS=0`). 60s = DT 5e-3 에서 12,000 스텝이라 기존 런에 16분을
더한다.

```
[phase] crush @done  크랭크 +2832.8deg (7.87 바퀴)
[crush] 정제 3.97x5.00x3.96 -> 3.98x5.00x3.95 mm
[timing] steps=964.8s (14430 steps, 66.9ms/step)  합계=975.8s
```

---

## 12. 회수장치 클램프를 현재 샘플백 규격으로

> **Q.** 회수 장치에서 클램프하는 코드가 있을텐데, 그거 지금 사용하고 있는
> 샘플백 규격으로 다시 테스트해보자

`recovery2_bag_clamp.py` 를 3건 수정했다.

1. **봉투** `_seal1mm` → `_sealslab3mm`. 평탄 실링부 폭은 둘 다 가장자리 8mm 로
   같아 `SEAL_BAND_WIDTH` 는 그대로. 바뀌는 건 두께 1.0 → **3.0mm**.
2. **천 물성**을 full_workflow 와 일치: `t=0.1mm, E=4.0e6` → `t=1.0mm, E=4.0e5`
   (`E×t = 400 N/m` 보존).
3. **압착 틈새를 고정 상수 → 관계식으로.** `CLAMP_SLIDER_MM=0.8` 은 옛 조합에 손으로
   맞춘 값이라, 봉투만 바꾸면 실효 두께가 1.2 → 5.0mm 가 되는데 틈새는 0.8 로 남아
   4.2mm 를 억지로 밀어넣게 된다.

```
실효 두께 = 실링부 자유 두께 + 2 × CLOTH_THICK
목표 틈새 = 실효 두께 − CLAMP_PRESS_MM(0.4)
```

회귀 확인: 예전 입력을 넣으면 실효 1.20 / 틈새 **0.80mm** 로 하드코딩 값과 정확히
일치 → 재튜닝이 아니라 리팩터.

**결과 — 시퀀스는 돌았고 파지는 실패**

```
[cfg] 실링 3.00mm + 2 x 1.00mm = 실효 5.00mm  ->  틈새 4.60mm (auto)
A. 턱 열림 +35.05mm   B. 실링부 하단 z 58.00mm ✓   C. 압착 311.1deg / +4.60mm ✓
D. 해제 후 낙하량 +36.47mm  (탄성 스냅 +3.33 / **미끄러짐 +33.14**)
```

새로 넣은 정합성 경고가 원인을 잡아냈다:

```
[경고] 씬 실측 실링두께 31.038mm 가 STL 값 3.000mm 와 다르다
```

상단 중앙만 잡고 매달린 봉투가 파우치처럼 벌어져 실링부 정점이 x 로 31mm
(해제 시점 52mm, 낙하 중 73mm)에 퍼져 있다. 턱은 4.60mm 로 닫는데 물어야 할 천이
그 앞 30~50mm 에 흩어져 있다. **틈새 값 문제가 아니라 투입 시점의 봉투 형상 문제다.**

---

## 13. 정제가 왜 안 맞는지 확인

> **Q.** 정제가 왜 안 맞는지 sideview로 확인해줘

**전제가 틀렸다 — 정제는 매 회전 맞고 있었다.** 로그 샘플 간격(2.0s)이 회전
주기(7.5s)보다 짧아 접촉 구간에 샘플이 걸릴 때만 잡혔던 것이다.

| 샘플 | 크랭크 360° 나머지 | 회전 | 정제 x |
|---|---|---|---|
| t=4.0s | 151.5° | 1번째 | 3.97 → **0.06mm** |
| t=12.0s | 169.3° | 2번째 | → **0.05mm** |
| t=20.0s | 193.3° | 3번째 | → **0.33mm** |
| t=28.0s | 217.2° | 4번째 | → 3.91mm (끝자락) |
| t=34.0s | 145.2° | 5번째 | → 3.35mm (진입) |
| t=42.0s | 169.1° | 6번째 | → **0.23mm** |
| t=50.0s | 193.1° | 7번째 | → **0.39mm** |

7회전 전부 크랭크 145~217° 구간에서 잡힌다 — 슬라이더 최대 전진(θ=180°) 부근으로
크랭크-슬라이더 기구상 맞는 위치다. `wall` 도 그때마다 −8.00 → −7.89mm 로 밀린다.

**진짜 문제는 정제가 완전 탄성체라는 것이다.**

```python
gs.materials.FEM.Elastic(E=5.0e4, nu=0.45, model="stable_neohookean")
```

소성도 파괴도 없어 매 타격 99% 압축(3.97 → 0.05mm)돼도 100% 원상복구된다.
8바퀴를 돌려도 최종 크기가 시작과 같을 수밖에 없다. E=50kPa 는 고무보다 무르다
(실제 정제는 GPa 급 취성). 이 시퀀스로 검증되는 건 "분쇄가 되는가"가 아니라
**"타격이 정확히 전달되는가"** 이고, 후자는 명확히 성립한다.

---

## 14. Crushing 영상이 어디까지 뽑혔나

> **Q.** 지금 Crushing 이 추가된 영상은 어디까지 뽑아졌지?

**60초 전부.** 영상 3종 모두 `01:12.14`.

```
RESULT/grip-28mm/full_workflow_grip-28mm_yoff+0.0mm_20260826_043355_{overview,bagcam,sideview}.mp4
```

크러시 전 12.15s + 크러싱 60.00s = **72.15s**, 영상 72.14s 로 시뮬 시간과 1:1.
크러싱 구간은 영상 **12.15s ~ 72.15s** 의 마지막 60초이고 크랭크 7.87바퀴가 다 담겼다.

프레임 매핑을 처음에 잘못 계산했는데, Genesis 가 `render()` 호출 수가 아니라
**시뮬 시간 페이스로 녹화**하기 때문이었다(크러시 없는 런: 12.15s × 30fps ≈ 347프레임).
`CRUSH_RENDER_EVERY=10` 은 렌더 호출을 줄여 속도만 벌었고 영상 길이에는 영향이 없다.

---

## 15. 공압그리퍼 위치 조정 (진행 중)

> **Q.** 공압그리퍼의 위치도 Full_sequence 내에서 조정해야한다. 위치는 샘플백의
> 중앙에 정확하게 공압 그리퍼의 양측면이 닿을 수 있도록 위치 조정

확인 결과 **석션V1 쪽 기구**였다. `석션V1.xml` 의 L/R 한 쌍이 공압 그리퍼다:

```
Dummy_1                              slide x  (리니어 스테이지)
├ L_E-SMLG9H-100-ES10_2_1            slide +y, range [-0.05, 0]
└ R_E-SMLG9H-100-ES10_2_1            slide -y, range [-0.05, 0]
   (equality 로 두 조가 동기 구동)
```

`E-SMLG9H-100-ES10` 은 SMC 공압 그리퍼 모델명이고, 두 조가 ±y 로 마주 보며 닫힌다 —
스케치와 일치한다.

**현재 배치의 문제**: `SUCTIONV1_POS` 는 **흡착컵** 중점을 회수장치2 F링크 중점에
맞춘 값이다(2026-07-29). 공압 그리퍼 턱을 봉투 중앙에 맞추려면 정렬 기준이 바뀌므로
흡착컵 정렬과 양립하지 않을 수 있다.

*여기서 세션이 중단됐다 — 정렬 기준을 어느 쪽으로 할지 결정이 필요하다.*

---

## 부록 — 이 세션에서 추가된 환경변수

| 변수 | 기본 | 뜻 |
|---|---|---|
| `LEFTWALL_SPLIT` | `1` | Left_Wall 충돌 분리 on/off |
| `CRUSH_SECONDS` | `0` | 크러셔 운전 시간(초). 0 이면 생략 |
| `CRUSH_RENDER_EVERY` | `10` | 크러시 구간 렌더 솎기 |
| `CRANK_TORQUE_LIM_NM` | `12.5` | 크랭크 토크 상한 |
| `CLAMP_MM` (recovery2) | `auto` | 압착 틈새. `auto` 면 실링 두께에서 산출 |
| `CLAMP_PRESS_MM` (recovery2) | `0.4` | 눌러 물 양 |
| `BAG_STL_NAME` (recovery2) | `_sealslab3mm` | 봉투 메시 |
| `CLOTH_THICK_MM` / `CLOTH_E` (recovery2) | `1.0` / `4.0e5` | 천 물성 |
